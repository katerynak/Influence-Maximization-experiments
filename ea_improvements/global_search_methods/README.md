Different global mutation techniques are compared here: 
    - random: random gene is selected for mutation
    - degree: the probability to select a gene is proportional to its approximated 
    second order degree
    - differential evolution ? 
